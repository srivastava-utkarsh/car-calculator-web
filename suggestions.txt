<!DOCTYPE html> <html lang="en"> <head> <meta charset="utf-8" /> <meta name="viewport" content="width=device-width, initial-scale=1" /> <title>Loan Prepayment Calculator</title> <style> :root{ --bg:#0d1117; --panel:#121826; --panel2:#0f1420; --text:#e6ecf3; --muted:#9ab1c9; --border:#1b2230; --accent:#4da3ff; --pos:#22c55e; --warn:#f59e0b; --radius:10px; --gap:12px; --pad:14px; } *{box-sizing:border-box} body{margin:0;background:var(--bg);color:var(--text);font:15px/1.45 Inter,system-ui,Segoe UI,Roboto,Arial} .wrap{max-width:1024px;margin:0 auto;padding:18px} h1{font-size:18px;margin:0 0 10px}
/* Cards and grids */
.card{background:linear-gradient(180deg,var(--panel) 0%,var(--panel2) 100%);border:1px solid var(--border);border-radius:var(--radius);padding:var(--pad)}
.title{font-size:14px;color:#cfe3ff;margin-bottom:8px;font-weight:600}
.row{display:grid;gap:var(--gap)}
.grid-3{grid-template-columns:1fr 1fr 1fr}
@media (max-width:900px){.grid-3{grid-template-columns:1fr}}

/* Inputs compact */
.fields{display:grid;gap:var(--gap)}
.field{display:flex;flex-direction:column;gap:6px}
.label{font-size:12px;color:#cbd6e5}
.ctrl{display:flex;gap:8px;align-items:center;background:#0b1220;border:1px solid var(--border);border-radius:8px;padding:10px}
input,select{background:transparent;border:0;outline:0;color:var(--text);width:100%;font-size:14px}
.prefix,.suffix{font-size:12px;color:var(--muted)}
.subnote{font-size:12px;color:#97a9bd;margin-top:8px}

/* Comparison section */
.compare{display:grid;gap:var(--gap)}
@media(min-width:900px){.compare{grid-template-columns:1fr 1fr}}
.panel{border:1px solid var(--border);border-radius:var(--radius);padding:16px;background:#0f1524}
.kicker{font-size:12px;color:#cbd6e5;margin-bottom:6px}
.big{font-weight:800;font-size:24px;margin:0 0 6px}
.meta{font-size:13px;color:var(--muted)}
.save{color:var(--pos);font-weight:700}
.emi-line{margin-top:6px;font-size:13px}
.emi-same{color:#b7d7ff}
.emi-diff .before{color:#5e86ff}
.emi-diff .after{color:#22c55e;font-weight:600}

/* Table compact */
table{width:100%;border-collapse:separate;border-spacing:0}
th,td{padding:10px 8px;border-bottom:1px solid var(--border);text-align:left}
th{font-size:11px;letter-spacing:.4px;color:#bcd1e6;text-transform:uppercase}
td{font-size:14px}
.pos{color:var(--pos);font-weight:600}
.warn{color:var(--warn);font-weight:600}

/* Footer actions */
.bar{display:flex;gap:8px;justify-content:flex-end;margin-top:10px}
.btn{background:#0f2436;border:1px solid #18324a;color:#cfe3ff;padding:8px 12px;border-radius:8px;cursor:pointer}
.btn--pri{background:#0461e6;border-color:#0c5ed1;color:#fff}
</style>

</head> <body> <div class="wrap"> <h1>Loan Prepayment Calculator</h1> <!-- Your Current Loan --> <section class="card"> <div class="title">Your Current Loan</div> <div class="fields grid-3"> <div class="field"> <div class="label">Loan Amount</div> <div class="ctrl"><span class="prefix">₹</span><input type="number" placeholder="800000" aria-label="Loan Amount" id="loanAmount"></div> <div class="subnote">Enter principal outstanding</div> </div> <div class="field"> <div class="label">Interest Rate</div> <div class="ctrl"><input type="number" step="0.05" placeholder="8.00" aria-label="Interest Rate" id="interestRate"><span class="suffix">% p.a.</span></div> </div> <div class="field"> <div class="label">Loan Tenure</div> <div class="ctrl"><input type="number" placeholder="3" aria-label="Loan Tenure Years" id="tenureYears"><span class="suffix">years</span></div> </div> </div> </section> <!-- Prepayment Configuration --> <section class="card" style="margin-top:12px"> <div class="title">Prepayment Configuration</div> <div class="fields grid-3"> <div class="field"> <div class="label">Frequency</div> <div class="ctrl"> <select aria-label="Prepayment Frequency" id="frequency"> <option>Yearly</option> <option>Quarterly</option> <option>Monthly</option> <option>One‑time (Lumpsum)</option> </select> </div> </div> <div class="field"> <div class="label">Prepayment Amount</div> <div class="ctrl"><span class="prefix">₹</span><input type="number" placeholder="16000" aria-label="Prepayment Amount" id="prepayAmount"></div> </div> <div class="field"> <div class="label">Prepayment Penalty (if any)</div> <div class="ctrl"><input type="number" step="0.1" placeholder="0" aria-label="Prepayment Penalty" id="penalty"><span class="suffix">%</span></div> <div class="subnote">Most floating‑rate loans have 0% penalty</div> </div> </div> </section> <!-- Comparison --> <section class="card" style="margin-top:12px"> <div class="title">Comparison</div> <div class="compare"> <!-- Without Prepayment --> <div class="panel" id="panelWithout"> <div class="kicker">Without Prepayment</div> <div class="big" id="tenureWithout">3 years</div> <div class="meta" id="interestWithout">Total interest: ₹1,02,487</div> <div class="emi-line emi-same" id="emiWithout">EMI: ₹25,069</div> </div> <!-- With Prepayment --> <div class="panel" id="panelWith"> <div class="kicker">With Prepayment</div> <div class="big" id="tenureWith">2 years 11 months</div> <div class="meta" id="saveLine">You save <span class="save">₹4,260</span> and finish 1 month earlier</div> <div class="emi-line emi-same" id="emiWith">EMI: ₹25,069</div> </div> </div> </section> <!-- Loan Details --> <section class="card" style="margin-top:12px"> <div class="title">Loan Details</div> <div style="overflow:auto"> <table> <thead> <tr><th>Metric</th><th>Before</th><th>After</th><th>Savings</th></tr> </thead> <tbody> <tr><td>Loan Tenure</td><td id="tblTenureBefore">3 years</td><td id="tblTenureAfter">2 years 11 months</td><td class="pos" id="tblTenureSave">1 month</td></tr> <tr><td>Total Interest Paid</td><td id="tblIntBefore">₹1,02,487</td><td id="tblIntAfter">₹98,227</td><td class="pos" id="tblIntSave">₹4,260</td></tr> <tr><td>Monthly EMI</td><td id="tblEmiBefore">₹25,069</td><td id="tblEmiAfter">₹25,069</td><td id="tblEmiSave">—</td></tr> <tr><td>Prepayment Penalty</td><td>—</td><td id="tblPenalty">₹0</td><td class="warn">—</td></tr> <tr><td>Total Amount Paid</td><td id="tblTotalBefore">₹9,02,487</td><td id="tblTotalAfter">₹8,98,227</td><td class="pos" id="tblTotalSave">₹4,260</td></tr> </tbody> </table> </div> <div class="bar"> <button class="btn" id="btnReset">Reset</button> <button class="btn">Share</button> <button class="btn">Download CSV</button> <button class="btn btn--pri" id="btnRecalc">Recalculate</button> </div> </section> </div> <script> // NOTE: This is a minimal demo calculation to update EMI labels and show "same vs different". // Replace with your full amortization + prepayment logic. const el = id => document.getElementById(id); const fmt = n => '₹' + Number(n).toLocaleString('en-IN'); function calcEMI(P, annualRate, years){ const r = (annualRate/12)/100; const n = years*12; if(r === 0) return P / n; return P * r * Math.pow(1+r, n) / (Math.pow(1+r, n)-1); } function recalc(){ const P = Number(el('loanAmount').value || 800000); const R = Number(el('interestRate').value || 8); const Y = Number(el('tenureYears').value || 3); // Base EMI const emi = Math.round(calcEMI(P,R,Y)); // Naive interest estimate (for demo) const totalPay = Math.round(emi * Y * 12); const totalInt = totalPay - P; // Pretend prepayment effect: reduce one month if prepayAmount > 0 const preAmt = Number(el('prepayAmount').value || 16000); const hasPrepay = preAmt > 0; const monthsSaved = hasPrepay ? 1 : 0; const tenureWithout = `${Y} ${Y===1?'year':'years'}`; const tenureWith = monthsSaved ? `${Y} years ${11} months` : tenureWithout; const saveAmt = hasPrepay ? 4260 : 0; const penaltyPct = Number(el('penalty').value || 0); const penalty = Math.round(preAmt * (penaltyPct/100)); // Update panels el('tenureWithout').textContent = tenureWithout; el('interestWithout').textContent = `Total interest: ${fmt(totalInt)}`; el('emiWithout').textContent = `EMI: ${fmt(emi)}`; el('tenureWith').textContent = tenureWith; el('saveLine').innerHTML = hasPrepay ? `You save <span class="save">${fmt(saveAmt)}</span> and finish ${monthsSaved} month earlier` : `No prepayment applied`; // Assume EMI unchanged for prepayment-to-principal const emiAfter = emi; // if your logic changes EMI, set here. // EMI line rule: same vs different if(emiAfter === emi){ el('emiWith').className = 'emi-line emi-same'; el('emiWith').textContent = `EMI is same: ${fmt(emiAfter)}`; }else{ el('emiWith').className = 'emi-line emi-diff'; el('emiWith').innerHTML = `EMI changed: <span class="before">${fmt(emi)}</span> → <span class="after">${fmt(emiAfter)}</span>`; } // Table el('tblTenureBefore').textContent = tenureWithout; el('tblTenureAfter').textContent = tenureWith; el('tblTenureSave').textContent = monthsSaved ? `${monthsSaved} month` : '—'; el('tblIntBefore').textContent = fmt(totalInt); el('tblIntAfter').textContent = fmt(totalInt - saveAmt); el('tblIntSave').textContent = saveAmt ? fmt(saveAmt) : '—'; el('tblEmiBefore').textContent = fmt(emi); el('tblEmiAfter').textContent = fmt(emiAfter); el('tblEmiSave').textContent = emiAfter===emi ? '—' : fmt(emi - emiAfter); el('tblPenalty').textContent = fmt(penalty); el('tblTotalBefore').textContent = fmt(totalPay); el('tblTotalAfter').textContent = fmt(totalPay - saveAmt + penalty); el('tblTotalSave').textContent = saveAmt ? fmt(saveAmt - penalty) : '—'; } // Events ['loanAmount','interestRate','tenureYears','prepayAmount','penalty','frequency'].forEach(id=>{ const node = el(id); if(node) node.addEventListener('input', ()=>{ requestAnimationFrame(recalc); }); if(node && node.tagName==='SELECT') node.addEventListener('change', recalc); }); el('btnRecalc').addEventListener('click', recalc); el('btnReset').addEventListener('click', ()=>{ el('loanAmount').value = 800000; el('interestRate').value = 8; el('tenureYears').value = 3; el('prepayAmount').value = 16000; el('penalty').value = 0; el('frequency').value = 'Yearly'; recalc(); }); // Initialize defaults el('loanAmount').value = 800000; el('interestRate').value = 8.00; el('tenureYears').value = 3; el('prepayAmount').value = 16000; el('penalty').value = 0; recalc(); </script> </body> </html>